import styled from 'styled-components';

export const Button = styled.button`
	display: flex;
	align-items: center;
	justify-content: center;

	position: fixed;
	right: 1.2rem;
	bottom: 1.2rem;

	font-size: 2.5rem;

	width: 5rem;
	height: 5rem;

	border-radius: 50%;

	background: var(--dark);
	color: var(--white);
`;
